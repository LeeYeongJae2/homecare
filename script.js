const header=document.querySelector('#header');const menuBtn=document.querySelector('#menuBtn');const nav=document.querySelector('#nav');window.addEventListener('scroll',()=>header.classList.toggle('scrolled',window.scrollY>20));menuBtn.addEventListener('click',()=>{const open=nav.classList.toggle('open');menuBtn.setAttribute('aria-expanded',String(open));menuBtn.setAttribute('aria-label',open?'메뉴 닫기':'메뉴 열기')});nav.querySelectorAll('a').forEach(a=>a.addEventListener('click',()=>{nav.classList.remove('open');menuBtn.setAttribute('aria-expanded','false')}));
const revealObserver=new IntersectionObserver(entries=>entries.forEach(entry=>{if(entry.isIntersecting){const delay=entry.target.dataset.delay||0;setTimeout(()=>entry.target.classList.add('visible'),delay);revealObserver.unobserve(entry.target)}}),{threshold:.13});document.querySelectorAll('.reveal').forEach(el=>revealObserver.observe(el));
// 시공 전후 슬라이더가 필요하면 이 위치에 기능을 추가하세요.
const track=document.querySelector('#reviewTrack');const cards=[...track.children];let reviewIndex=0;function cardsVisible(){return innerWidth<=600?1:innerWidth<=900?2:3}function moveReviews(){const visible=cardsVisible();reviewIndex=Math.max(0,Math.min(reviewIndex,cards.length-visible));const gap=20;const cardWidth=(track.parentElement.clientWidth-gap*(visible-1))/visible;track.style.transform=`translateX(-${reviewIndex*(cardWidth+gap)}px)`}document.querySelector('#reviewPrev').addEventListener('click',()=>{reviewIndex--;moveReviews()});document.querySelector('#reviewNext').addEventListener('click',()=>{reviewIndex++;moveReviews()});window.addEventListener('resize',moveReviews);
const form=document.querySelector('#estimateForm');const toast=document.querySelector('#toast');form.addEventListener('submit',event=>{event.preventDefault();toast.textContent='상담 폼 화면이 정상 작동합니다. 실제 전송 연동이 필요합니다.';toast.classList.add('show');setTimeout(()=>toast.classList.remove('show'),3500)});
